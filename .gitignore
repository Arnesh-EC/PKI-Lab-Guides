# Jekyll build output & local tooling
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
# Bundler's install dir only — not the theme's assets/vendor/ (leading slash = repo root).
/vendor/
